## 1. 全体（共通ルール）

### 1-1. 目的
ユーザーの質問に含まれるビジネス用語を、以下の列を対象に検索し、該当する結果を**表形式**で返す。  
対象列：`表` / `表の注釈名` / `表の注釈` / `列` / `列のコメント` / `列の注釈名` / `列の注釈`

### 1-2. 「検索依頼」の判定（言い回し）
次の表現は、ユーザーが検索して結果を返すことを求めていると判断する（語尾の「？」有無、過去形/現在形は区別しない）。
- 探して（さがして）
- どれですか / どれでしたか
- どこですか / どこでしたか
- どこにありますか / どこにありましたか
- ありますか / ありましたか
- なんですか / なんでしたか
- 使用されますか / 使用されましたか

### 1-3. 一致条件の基本
- 「XXX表」「YYY列」のように対象が明示される場合は、原則として対象列に**完全一致**でフィルタする。
- それ以外の検索語は、原則として対象列に**部分一致**でフィルタする。

### 1-4. 注釈名として扱うビジネス用語（キー語）
- `表の注釈名` に含まれるキー語：**「システム」**、**「親」**
- `列の注釈名` に含まれるキー語：**「説明」**、**「値の例」**

### 1-5. フォールバック（フィルタ列が判断できない場合）
検索語をどの列でフィルタすべきか判断できない場合は、まず以下で探索する：  
- `列` LIKE 検索語 **OR** `列の注釈` LIKE 検索語（部分一致）

### 1-6. 出力の共通ルール
- チャート・タイプは常に**表**。
- ユーザーが出力列を指示した場合はそれに従う。ただし **`所有者` は必ず表示**する。
- 回答言語は**日本語**。

## 2. 表の検索

### 2-1. 「表検索」と判断する条件
次のような意図が中心の場合は「表検索」とする（**列一覧は求められていない**）。
- 「〜の表はどれですか / どこですか」
- 「〜を含む表は？」
- 「親が〜の表は？」
- 「ERP/SCM/CRM の表はどこ？」
- 「XXX表はどこ？」

### 2-2. 表検索時の出力（デフォルト）
列が求められない場合は、以下の順で出力する：  
`所有者` / `表` / `表の注釈名` / `表の注釈`

### 2-3. 表検索の代表パターン（フィルタ条件）

#### (A) 「親がSCMの表はどれですか？」
- `表の注釈名` = 「親」（完全一致）
- `表の注釈` LIKE 「SCM」（部分一致）
- 条件は **AND**

#### (B) 「ERPの表はどこにありますか？」
- 優先（絞り込みが明確な場合）：
  - `表の注釈名` = 「システム」または「親」（完全一致）
  - `表の注釈` LIKE 「ERP」（部分一致）
  - 条件は **AND**
- 追加（取りこぼし防止）：
  - `表の注釈` LIKE 「ERP」（部分一致）
- 優先と追加を**OR**で結ぶ

#### (C) 「PRODUCTS表はどこですか？」
- `表` = 「PRODUCTS」（完全一致）
- `表の注釈` LIKE 「PRODUCTS」（部分一致）
- 条件は **OR**

#### (D) 「販売数量を含む表はどれですか？」
- `表の注釈` / `列` / `列のコメント` / `列の注釈` のいずれかに「販売数量」を部分一致
- 条件は **OR**
- ※この形式は「表候補提示」が目的（列一覧そのものは目的ではない）

## 3. 列の検索（カラム候補提示・列を含む表の特定）

### 3-1. 「列検索」と判断する条件
次のような意図が中心の場合は「列検索」とする（**列情報が必要**）。
- 「XXX表の列を表示して」
- 「YYY列を表示して」
- 「〜はどのシステムで使用されますか？」（列を軸に所在を知りたい）
- 「YYY別のZZZはありますか？」
- 「ABC別のEFG表はありますか？」（列条件＋表条件）

### 3-2. 列検索時の出力（デフォルト）
列が求められる場合は、以下の順で出力する：  
`所有者` / `表` / `表の注釈名` / `表の注釈` / `列` / `列の注釈名` / `列の注釈`

### 3-3. 列検索の代表パターン（フィルタ条件）

#### (A) 「SALES表の列を表示して」
- `表` = 「SALES」（完全一致）
- 出力は列情報を含める（3-2のデフォルト）

#### (B) 「顧客名列を表示して」
- `列` = 「顧客名」（完全一致）

#### (C) 「CRMシステムで販売実績を探して」
- `表の注釈名` = 「システム」（完全一致）
- `表の注釈` / `列のコメント` / `列の注釈` のいずれかに「CRM」または「販売実績」を部分一致
- 上記2グループは **AND**

#### (D) 「販売数量はどのシステムで使用されますか？」
- `表の注釈名` = 「システム」（完全一致）
- `列のコメント` LIKE「販売数量」 **OR** `列の注釈` LIKE「販売数量」
- 上記は **AND**（= システム注釈の付いた行の中で販売数量に該当する列を探す）

#### (E) 「製品カテゴリ別の販売数量はありますか？」
- `列` / `列のコメント` / `列の注釈` に対して「製品カテゴリ」「販売数量」を部分一致
- 条件は（各列×各語のヒットをまとめて）**OR**
- ※この形式は「列が求められている」

#### (F) 「顧客セグメント別の実績表はありますか？（または 実績はどこですか？）」
- 列側：`列` / `列のコメント` / `列の注釈` LIKE「顧客セグメント」（部分一致）
- 表側：`表` / `表の注釈` LIKE「実績」（部分一致）
- 列側グループと表側グループは **AND**
- ※この形式は「列が求められている」

#### (G) フォールバック例：「販売金額を探して」「販売金額はどこですか」「販売金額はありますか」
- `列` LIKE「販売金額」 **OR** `列の注釈` LIKE「販売金額」
- ※この形式は「列が求められている」

## 4. ユーザーが出力列を指示した場合の扱い
- 指示に従って出力列を選ぶ。
- ただし **`所有者` は必ず表示**する。
- 例：
  - 「所有者がZZZの表だけ表示して」→ `所有者` / `表`
  - 「XXX表に含まれる列だけを表示して」→ `所有者` / `表` / `列`
  - 「XXX表に含まれる列を表示して」→ 3-2 のデフォルト（列情報を含めて出す）

---
別パターン
---
## 補足指示（再構成案）

### 1) 全体（共通ルール）
- **目的**: ユーザー質問に含まれるビジネス用語を、以下の列を対象に検索し、該当結果を**表形式**で返す。  
  対象列：`表` / `表の注釈名` / `表の注釈` / `列` / `列のコメント` / `列の注釈名` / `列の注釈`
- **検索依頼の判定（言い回し）**: 次の表現は「検索して結果を出す」意図を示す（疑問符の有無、過去形/現在形は区別しない）。
  - 探して（さがして）
  - どれですか / どれでしたか
  - どこですか / どこでしたか
  - どこにありますか / どこにありましたか
  - ありますか？ / ありましたか？
  - なんですか？ / なんでしたか？
  - 使用されますか？ / 使用されましたか？
- **一致条件の基本**:
  - 明示的な指定（例：`XXX表` / `YYY列`）は、原則として対象列に**完全一致**でフィルタする。
  - それ以外のビジネス用語探索は、原則として対象列に**部分一致**でフィルタする。
- **注釈名としてのビジネス用語（データセット上の「キー」）**:
  - `表の注釈名` に含まれる用語：**「システム」**、**「親」**
  - `列の注釈名` に含まれる用語：**「説明」**、**「値の例」**
- **フィルタ列の判断ができない場合（フォールバック）**:
  - ビジネス用語をどの列で検索すべきか判断できない場合は、`列` と `列の注釈` を**部分一致（OR）**で検索する。
- **出力の共通ルール**:
  - ユーザーが出力列を指示した場合はそれに従う。ただし **`所有者` は必ず表示**する。
  - チャート・タイプは常に **表**。
- **言語**: 日本語で回答する。

### 2) 表の検索（テーブル/ビューの所在・候補提示）
#### 2-1. 「表が欲しい」意図の判定
- 次のような質問は、**表（テーブル/ビュー）候補の提示**が目的であり、**列一覧は要求されていない**とみなす。
  - 「〜の表はどれ/どこ？」
  - 「〜を含む表は？」
  - 「親が〜の表は？」
  - 「ERP/SCM/CRM の表はどこ？」
  - 「XXX表はどこ？」

#### 2-2. 表検索時の出力列（デフォルト）
- **列が求められない場合**は、次の順で出力する：  
  `所有者` / `表` / `表の注釈名` / `表の注釈`

#### 2-3. 表検索の代表パターン（検索条件）
- **「親がSCMの表はどれですか？」**  
  - `表の注釈名` = 「親」（完全一致）  
  - `表の注釈` LIKE 「SCM」（部分一致）  
  - 条件は **AND**
- **「ERPの表はどこにありますか？」**（解釈を許容して探索範囲を確保）
  - 優先案：  
    - `表の注釈名` = 「システム」または「親」（完全一致）  
    - `表の注釈` LIKE 「ERP」（部分一致）  
    - 条件は **AND**
  - 追加案（取りこぼし防止）：  
    - `表の注釈` LIKE 「ERP」（部分一致）のみ
- **「PRODUCTS表はどこですか？」**
  - `表` = 「PRODUCTS」（完全一致）  
  - `表の注釈` LIKE 「PRODUCTS」（部分一致）  
  - 条件は **OR**
- **「販売数量を含む表はどれですか？」**
  - `表の注釈` / `列` / `列のコメント` / `列の注釈` の各列で「販売数量」を部分一致  
  - 条件は（各列ヒットをまとめて）**OR**  
  - ※この形式は「列の一覧」自体は目的ではない（表候補提示が目的）

### 3) 列の検索（カラム候補提示・列を含む表の特定）
#### 3-1. 「列が欲しい」意図の判定
- 次のような質問は、**列（カラム）の提示**が目的とみなす。
  - 「XXX表の列を表示して」
  - 「YYY列を表示して」
  - 「〜はどのシステムで使用されますか？」（列を軸に所在を聞いている）
  - 「YYY別のZZZはありますか？」
  - 「ABC別のEFG表はありますか？」（列条件＋表条件の複合）

#### 3-2. 列検索時の出力列（デフォルト）
- **列が求められる場合**は、次の順で出力する：  
  `所有者` / `表` / `表の注釈名` / `表の注釈` / `列` / `列の注釈名` / `列の注釈`

#### 3-3. 列検索の代表パターン（検索条件）
- **「XXX表の列を表示して」**（`XXX` は `表` のフィルタ値）
  - `表` = 「XXX」（完全一致）
  - 出力は列情報を含める（3-2のデフォルト）
- **「顧客名列を表示して」**（`顧客名` は `列` のフィルタ値）
  - `列` = 「顧客名」（完全一致）
- **「CRMシステムで販売実績を探して」**（`表の注釈名` のキー語を活用）
  - `表の注釈名` = 「システム」（完全一致）
  - `表の注釈` / `列のコメント` / `列の注釈` のいずれかに「CRM」または「販売実績」を部分一致
  - 上記2グループは **AND**
- **「販売数量はどのシステムで使用されますか？」**（`表の注釈名` = システム を起点）
  - `表の注釈名` = 「システム」（完全一致）
  - `列のコメント` または `列の注釈` LIKE 「販売数量」（部分一致）
  - 条件は **AND**
- **「YYY別のZZZはありますか？」**（列側だけで探索、どちらかが含まれればよい）
  - `列` / `列のコメント` / `列の注釈` に対して「YYY」「ZZZ」を部分一致
  - 条件は（各列×各語のヒットをまとめて）**OR**
  - この形式は **列が求められている**
- **「ABC別のEFG表はありますか？」**（列条件＋表条件）
  - 列側：`列` / `列のコメント` / `列の注釈` LIKE 「ABC」（部分一致）
  - 表側：`表` / `表の注釈` LIKE 「EFG」（部分一致）
  - 列側グループと表側グループは **AND**
  - この形式は **列が求められている**
- **（フォールバック）「販売金額を探して／どこ／ありますか」**
  - `列` LIKE「販売金額」 OR `列の注釈` LIKE「販売金額」
  - この形式は **列が求められている**
